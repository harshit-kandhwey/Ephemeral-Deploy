# Core
flask==3.1.3
flask-sqlalchemy==3.1.1
flask-migrate==4.0.5
flask-jwt-extended==4.7.4
flask-cors==6.0.0
flask-limiter==4.1.1

# Database
psycopg2-binary==2.9.9
alembic==1.18.5

# Redis & Celery
redis==8.0.1
celery==5.6.3

# AWS — keep boto3 and botocore on the same version: boto3 X.Y.Z requires
# botocore >=X.Y.Z,<X.(Y+1).0, so bumping one without the other is
# unresolvable for pip (this is what broke the dependabot boto3 PR)
boto3==1.43.44
botocore==1.43.44

# Security
bcrypt==5.0.0
python-dotenv==1.2.2

# API Documentation
flasgger==0.9.7.1

# Monitoring
prometheus-client==0.25.0

# Server
gunicorn==23.0.0

# Utilities
marshmallow==4.3.0
python-dateutil==2.9.0.post0

# Testing
pytest==9.1.1
pytest-cov==4.1.0